# soRai

Subsystem-by-subsystem rebuild of the content-engine and content-engine-service stack.

## Checkpoints

1. `content-engine` renderer with explicit `frontmatter.yaml`.

Run the current checkpoint:

```bash
cd content-engine
tests/run_all.sh
```
